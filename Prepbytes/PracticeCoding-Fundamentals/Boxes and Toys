#include <bits/stdc++.h>
using namespace std;

int main()
{
  int n,k=0;
  cin>>n;
  
  for(int i=1;i<=n;i++){
    int t,c;
    cin>>t>>c;
    
    if((c-t)>=2){
      k++;
    }
  }
  
  cout<<k;
  return 0;
}
