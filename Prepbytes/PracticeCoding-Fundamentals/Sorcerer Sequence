#include <bits/stdc++.h>
using namespace std;

int main()
{
  int t;
  cin>>t;
  while(t--){
    long long n;
    cin>>n;
 
    cout<<n<<" "; 
    
    while(n>1){
      if(n%2==0){
        n=sqrt(n);
      }
      else{
        n=sqrt(n)*sqrt(n)*sqrt(n);
      }
      cout<<n<<" ";
    }
    cout<<endl;
  }
  
  return 0;
}
